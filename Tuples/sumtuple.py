def sum_of_tuple(tup):
    """
    This function takes a tuple as input and returns the sum of its elements.
    """
    return sum(tup)

# Example usage
numbers = (1, 2, 3, 4, 5)
result = sum_of_tuple(numbers)
print("The sum of the tuple elements is:", result)  # Output: 15
